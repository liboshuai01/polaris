spring:
  cloud:
    nacos:
      discovery:
        server-addr: liboshuai.com:8848
        namespace: pulsar-dev
        group: DEFAULT_GROUP
      config:
        server-addr: liboshuai.com:8848
        file-extension: yaml
        namespace: pulsar-dev
        group: DEFAULT_GROUP
