spring:
  cloud:
    nacos:
      discovery:
        server-addr: liboshuai.com:18848
        namespace: pulsar-dev
        group: DEFAULT_GROUP
      config:
        server-addr: liboshuai.com:18848
        file-extension: yaml
        namespace: pulsar-dev
        group: DEFAULT_GROUP
